package com.agriknowledge.material;

/** Matches the discriminator values stored in materials.type. */
public enum MaterialType {
	ARTICLE,
	VIDEO,
	QUIZ,

	/** A PDF, currently used for previous year papers. */
	DOCUMENT
}
